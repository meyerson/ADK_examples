# Example environment configuration for Reluctant Agent
# Copy to .env and fill values as needed.

# Google Cloud project (optional if set via gcloud config)
GOOGLE_CLOUD_PROJECT=your-project-id

# Region or location (if required by ADK)
GOOGLE_CLOUD_REGION=us-central1

# Additional agent/tool settings can go here
