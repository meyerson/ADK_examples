# Per-agent environment (optional)
# These are read by the container via --env-file when present.
# Do NOT put secrets here; use Secret Manager for API keys.

GOOGLE_CLOUD_PROJECT=your-project-id
GENAI_API_KEY_SECRET_NAME=genai-api-key
AGENT_NAME=agent_w_tools
